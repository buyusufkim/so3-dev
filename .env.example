# Frontend env must never contain DB/API secrets.
# Backend config is handled by PHP private configuration (e.g. config.local.php).
# This file is for non-sensitive public frontend variables only (if needed).

